/**
 * @default 2 * Math.PI
 */
export const TAU = 2 * Math.PI

export const enum Walls {
  Top = 'wall-top',
  Right = 'wall-right',
  Bottom = 'wall-bottom',
  Left = 'wall-left',
}

export const enum Points {
  Ball = 'point-ball',
}

export const enum Colors {
  Tomato = 'rgba(255, 99, 71, 1)',
  TomatoTransparent = 'rgba(255, 99, 71, 0.4)',
}

export const enum LineWidth {
  Default = 1,
  Thin = 0.5,
  Thick = 2,
  ExtraThick = 4,
}
