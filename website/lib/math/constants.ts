/**
 * @default 2 * Math.PI
 */
export const TAU = 2 * Math.PI

export const enum Walls {
  Top = 'wall-top',
  Right = 'wall-right',
  Bottom = 'wall-bottom',
  Left = 'wall-left',
}

export const enum Points {
  Ball = 'point-ball',
}
